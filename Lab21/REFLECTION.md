1.  Section 4 with name being namee is tricky just because when debugging its easy to gloss over a simple spelling mistake, which I make all the time when coding. 

2. The cost of this design is needing to know the correct output and different cases in which the output is not set to one type. This can get a little tedious. Python handles types dynamically where java does it during compileing.

3. For lab 18's I would do a TypedDict because its more set in stone and its a record, where as lab 20's I would do a dict[K,V] because its more dynamic.